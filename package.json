{
  "name": "peer-server",
  "version": "1.0.0",
  "description": "Simple peer server for peerjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "peerjs --port 3000"
  },
  "author": "Shree Krishna Lamichhane",
  "license": "MIT",
  "dependencies": {
    "peer": "^0.6.1"
  }
}
